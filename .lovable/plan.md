## Remove Task 5D.1 — Personal Stats Dashboard

You want to drop the personal stats dashboard from the roadmap since you won't be collecting that data.

### Changes

1. `**docs/tasks.md**` — Remove row 5D.1 from the Phase 5D table. If 5D.2 (streak tracking) also depends on the same data, consider removing the entire Phase 5D section. Otherwise, keep 5D.2 as a standalone item.

No code changes, no database changes. Just documentation cleanup.