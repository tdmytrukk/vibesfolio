

## Update: Mark Dark Mode as Done

Task 6.7 in `docs/tasks.md` — "Dark mode toggle in UI" — needs to be marked ✅. The project already has dark mode CSS tokens, the `.dark` class overrides in `index.css`, and `next-themes` is installed. 

### Change
- **`docs/tasks.md` line 74**: Change `⬜` to `✅` and update the notes to reflect that dark mode is already implemented with `next-themes` and full CSS custom property support.

